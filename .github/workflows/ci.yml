name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  shellcheck:
    name: ShellCheck Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run shellcheck on all scripts
        run: |
          for f in *.sh api/*.sh init.d/* tests/*.sh; do
            if [ -f "$f" ]; then
              shellcheck -s bash -x -S error "$f" || exit 1
            fi
          done

  api-contract:
    name: API Contract Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run web panel tests
        run: bash tests/run_tests.sh

  frontend:
    name: Frontend Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint frontend
        run: npm run lint

      - name: Run frontend tests
        run: npm run test

      - name: Build frontend
        run: npm run build

      - name: Verify committed dist matches build output
        run: git diff --exit-code -- dist

      - name: Smoke check dist and README
        run: |
          test -f dist/index.html
          test -f dist/app.js
          test -f dist/app.css
          test -f dist/favicon.ico
          grep -q '/ruijie-cgi/status' README.md
          grep -q '/ruijie-cgi/account' README.md
          grep -q '/ruijie-cgi/settings' README.md
          grep -q '/ruijie-cgi/log' README.md
          grep -q '/ruijie-cgi/health' README.md
          grep -q '/ruijie-cgi/health-log' README.md
          grep -q '/ruijie-cgi/runtime' README.md
